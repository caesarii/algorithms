
const insertSorting = (arr) => {
    for(let index = 1; index < arr.length; index++) {
        const curr = arr[index]
        for(let before = index; before > 0; before --) {
            if(arr[before] > curr) {

            }
        }
        while (before > 0) {
            if(arr[before] > curr) {

            }
        }
    }
}